services:
  relay:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8765:8765"
    environment:
      - RELAY_HOST=0.0.0.0
      - RELAY_PORT=8765
      # Player ID registry persists across restarts.  The container runs as
      # nobody, so the host dir must be writable by it:  chown -R nobody ./data
      - RELAY_REGISTRY=/data/registry.json
    volumes:
      - ./data:/data
